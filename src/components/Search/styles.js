import makeStyles from '@material-ui/core/styles/makeStyles'

export default makeStyles(theme => ({
  search: {
    // position: 'fixed',
    height: '100%',
    // width: '100%',
    margin: 0,
    overflowX: 'auto',
  },
}))
